version: "3.8"

services:
  bot:
    container_name: discord-bot
    build: .
    env_file:
      - .env
    command: npm run start
    restart: unless-stopped

  deploy:
    build: .
    env_file:
      - .env
    command: npm run deploy